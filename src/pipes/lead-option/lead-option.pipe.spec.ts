import { LeadOptionPipe } from './lead-option.pipe';

describe('LeadOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
